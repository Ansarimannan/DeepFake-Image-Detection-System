[pytest]
# `tests/` imports `tests.conftest` for shared constants, and every test imports
# the package from `src/`, so both roots are on the path.
pythonpath = src .
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
